pipeline {
    agent any
    environment {
        PATH = "/opt/maven3/bin:$PATH"
    }
    stages {
        stage('cleanup') {
            steps {
                cleanWs()
            }
        }
        stage('checkout') {
            steps {
                git branch: 'main', url:'https://github.com/Rolex960/task_1'
            }
        }
        stage('Maven Build') {
            steps {
                 sh "mvn clean package"
            }
        }
    }
}
